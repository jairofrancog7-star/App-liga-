/* V13 disabled.
   Clasificación now uses the V12 reference table (with FORMA colors/status)
   to match the user's approved screenshot. Kept as an inert compatibility
   module so older cached HTML cannot re-render the Herrera/Terrícolas table. */
export {};
