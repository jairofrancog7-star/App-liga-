(()=>{'use strict';const root=document.documentElement;const top=document.querySelector('.topbar');if(top)top.classList.add('v7-topbar');const launch=document.getElementById('v7Launch');if(!launch)return;const orbit=launch.querySelector('.v7-orbit');const trophy=launch.querySelector('.v7-trophy');let done=false;const finish=()=>{if(done)return;done=true;launch.classList.add('v7-done');setTimeout(()=>launch.remove(),520)};const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;const t1=setTimeout(()=>{orbit?.classList.remove('active');trophy?.classList.add('active')},reduce?450:1350);const t2=setTimeout(finish,reduce?1050:3000);launch.querySelector('[data-v7-skip]')?.addEventListener('click',()=>{clearTimeout(t1);clearTimeout(t2);finish()});window.addEventListener('error',()=>{setTimeout(finish,250)},{once:true});setTimeout(()=>root.classList.add('v7-ready'),20)})();